@*@using WebSiteBanHang.Models

<div class="header-top">
	<div class="container">
		<div class="search">
			@using (Html.BeginForm("LayTuKhoaTimKiem", "TimKiem"))
			{
				<input type="text" name="sTuKhoa" placeholder="Nhập tên sản phẩm cần tìm..."/> //name phải trùng với tham số truyền vào trong hàm controller để nhận giá trị
				<input type="submit" value="Tìm kiếm"/>
			}
		</div>
		<div class="header-left">
			<ul>
				@if (Session["TaiKhoan"] != null && Session["TaiKhoan"] != "")
				{
					ThanhVien tv = (ThanhVien)Session["TaiKhoan"];
					<li>@tv.HoTen</li>
					@Html.ActionLink("Đăng xuất", "DangXuat", "Home");
				}
				else
				{
					<li style="display: inline;"><a id="btnDangNhap" class="lock" href="#">Đăng nhập</a></li>
					<li style="display:inline;"><a class="lock" href="#">Đăng ký</a></li>
				}
			</ul>
			@Html.Action("GioHangPartial", "GioHang");
			@*<div class="cart box_1">
					<a href="checkout.html">
						<h3>
							<div class="total">
								<span class="simpleCart_total"></span> (<span id="simpleCart_quantity" class="simpleCart_quantity"></span> items)
							</div>
							<img src="images/cart.png" alt="" />
						</h3>
					</a>
					<p><a href="javascript:;" class="simpleCart_empty">Empty Cart</a></p>

				</div>*@
			<div class="clearfix"> </div>
		</div>
		<div class="clearfix"> </div>
	</div>
</div>
<link href="~/Content/css/style_form_dangnhap.css" rel="stylesheet" />
<div>
	@*Tạo 1 div bên ngoài chứa div login-form *@
	<div id="popup_login">
		<div class="login-form" id="login-form">
			<div style="clear:both;text-align:right;margin-bottom:0;">
				<span id="close-form" class="btn btn-danger">X</span>
			</div>
			<h2>Đăng nhập</h2>
			<div class="form-info">
				@using (Ajax.BeginForm("DangNhap", "Home", new AjaxOptions { InsertionMode = InsertionMode.Replace, UpdateTargetId = "ThongBao" }))
				{
					<input class="email TaiKhoan" name="txtDangNhap" placeholder="Tên đăng nhập" required="" type="text">
					<input class="password MatKhau" name="txtMatKhau" placeholder="Mật khẩu" required="" type="password">
					<p><a href="#">Quên mật khẩu?</a></p>
					<h3 style="font-weight:bold;color:red;" id="ThongBao">Thông báo nó sẽ thay thế đoạn này</h3>
					<ul class="login-buttons">
						<li><input value="Đăng nhập" type="submit" id="btnDangNhap1"></li>
						<li><a href="#" class="hvr-sweep-to-left">Đăng ký</a></li>

					</ul>
				}
			</div>
		</div>
	</div>
</div>
<style>
	#btnDangNhap {
		float: left;
	}

	.content {
		height: 500px;
	}

	#close-form {
		color: white;
	}


	#login-form {
		background: #3a3a46;
		cursor: pointer;
	}
	/*css cho phần div popup_login*/
	#popup_login {
		position: fixed;
		top: 0;
		left: 0;
		width: 100%;
		height: 100%;
		background-color: rgba(0,0,0,.5); /*red, green, blue, còn a độ trong suốt (0-->1)*/
		z-index: 99; /*z-index càng lớn thì càng gần người dùng*/
		display: none;
	}
</style>
@*Sử dụng jquery hiển thị popup đăng nhập*@
<script type="text/javascript">
	//Hiển thị popup_login
	//$("#btnDangNhap").click(function () {
	//    $("#popup_login").show();
	//});
	$("#btnDangNhap").click(function () {
		$("#popup_login").fadeIn(500);/*độ trễ khi show lên*/
	})
	//$("#btnDangNhap").click(function () {
	//	$("#popup_login").slideDown();
	//})
	/*Thay đổi nhiều thuộc tính css*/
	//$("#btnDangNhap").click(function () {
	//	$("#popup_login").css({
	//		"display": "block",
	//		"background-color":"red"
	//	})
	//})
	//Ẩn đi popup_login
	$("#close-form").click(AnForm);

	//Định nghĩa function ẩn form
	function AnForm() {
		$("#popup_login").hide();
	}
	//Ví dụ về lấy giá trị từ textbox
	$("#btnDangNhap1").click(function () {
		var TaiKhoan = $(".TaiKhoan").val();
		var MatKhau = $(".MatKhau").val();
		//alert("Tài khoản:" + TaiKhoan + "\n Mật khẩu:" + MatKhau);
		if (TaiKhoan == "") {
			alert("Lỗi");
			return;
		}
	});
</script>*@